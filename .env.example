# Wallet that pays for each Telegraph request. TESTNET ONLY.
# Needs a small amount of Base Sepolia USDC (each answer costs $0.01).
EVM_PRIVATE_KEY=0xyour_base_sepolia_test_key

# Optional overrides
PORT=3000
TELEGRAPH_ENGINE_URL=https://devnode.telegraphprotocol.com/engine
ASK_TIMEOUT_MS=90000
STATS_FILE=data/stats.json
